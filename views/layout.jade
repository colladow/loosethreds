!!! 5
html
  head
    title loosethreds
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    != messages()

    nav
      a(href="/") Home
      - if(typeof currentUser === 'undefined')
        a(href="/login") Login
      - else
        a(href="/logout") Logout
        a(href="/users/#{currentUser.path}") My Account

    != body
